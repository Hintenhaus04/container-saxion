apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-blue
  namespace: app-prod
  labels:
    app: my-app
    version: blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-app
      version: blue
  template:
    metadata:
      labels:
        app: my-app
        version: blue
    spec:
      containers:
        - name: app
          image: europe-west1-docker.pkg.dev/public-cloud-concept/week3-repo/app-blue:latest
          ports:
            - containerPort: 80
