apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: app-prod
spec:
  type: LoadBalancer
  selector:
    app: my-app
    version: blue   # later naar green zetten
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
